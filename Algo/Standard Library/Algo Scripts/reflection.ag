// The "reflection" standard library, provided by Algo.
// Contains the "reflection" namespace.
// v0.0.1, revision 0.

library reflection
{
	// Get Variable (generic cast from String to Variable)
	external gvar <- std_reflection.gvar;

	// Create Variable (from string)
	external cvar <- std_reflection.cvar;

	// Set Variable (from string)
	external svar <- std_reflection.svar;

	// Delete Variable (from string)
	external dvar <- std_reflection.dvar;
}