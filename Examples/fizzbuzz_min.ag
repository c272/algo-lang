import "core";
import "io";

let limit = int(input.get());
for (i up to limit) 
{
    let out = "";
    if (i % 3 == 0) {
        out += "Fizz";
    }
    if (i % 5 == 0) {
        out += "Buzz";
    }
    
    if (out == "") { out = i; }
    print out;
}
